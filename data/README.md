# Data Workspace

## Task

Describe local raw, processed and blind-test datasets. Dataset files should remain outside Git unless explicitly approved.

## Technologies

NPZ, CSV or Parquet with JSON metadata.

## Track

Sequence ID, scenario, seed, split, frame count, labels, configuration hash and generation commit.
